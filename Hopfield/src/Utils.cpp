#include "Utils.h"
#include <algorithm>

std::vector<short> shufflePattern(const std::vector<short>& pattern, size_t shufflePercent)
{
    size_t patternSize = pattern.size();

    std::vector<short> shuffled = pattern;

    for (size_t i = 0; i < patternSize; i++)
    {

    }

    return shuffled;
}
